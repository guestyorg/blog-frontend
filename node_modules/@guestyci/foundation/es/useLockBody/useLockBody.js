import { useEffect } from 'react';

const useLockBody = lock => {
  useEffect(() => {
    document.body.style.overflow = lock ? 'hidden' : '';
    return () => {
      document.body.style.overflow = '';
    };
  }, [lock]);
};

export default useLockBody;