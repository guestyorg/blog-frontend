import React from 'react';
import { isEmpty } from "../utils";
import Divider from "../Divider";

const DropdownSelectedMenuList = ({
  children,
  onChange
}) => {
  const handleItemClick = (e, menuItemValue) => {
    e.stopPropagation();
    onChange(e, menuItemValue);
  };

  return /*#__PURE__*/React.createElement(React.Fragment, null, React.Children.map(children, child => React.cloneElement(child, {
    selected: true,
    onClick: handleItemClick,
    multiselect: true
  })), !isEmpty(children) && /*#__PURE__*/React.createElement(Divider, null));
};

export default DropdownSelectedMenuList;