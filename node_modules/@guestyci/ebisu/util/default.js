const BE = {
  baseImage: 'node-12',
  tags: {
    code: '(config|exec|package*.json|business|.circleci|swagger)',
    manifest: '.',
  },
};

const triggers = {
  commitMessageTag: '[build]',
};

module.exports = {
  BE,
  triggers,
};
