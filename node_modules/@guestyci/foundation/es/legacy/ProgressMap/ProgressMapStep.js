import React from 'react';
import cn from 'classnames';
import PropTypes from 'prop-types';
import withStyle from 'react-jss';
const styles = {
  progressMapStep: {
    minWidth: '64px',
    minHeight: '64px',
    borderRadius: '50%'
  }
};

const ProgressMapStep = ({
  index,
  item,
  color,
  classes
}) => /*#__PURE__*/React.createElement("div", {
  key: index,
  className: cn('flex-center', 'font-size-2xl', 'b-2', 'progress-map-step', classes.progressMapStep, `text-${color}`),
  "data-qa": "progress-map-step"
}, item);

ProgressMapStep.defaultProps = {
  color: 'blue'
};
ProgressMapStep.propTypes = {
  /** number for the range of steps */
  item: PropTypes.number.isRequired,

  /** color for step */
  color: PropTypes.oneOf(['blue', 'gray'])
};
export const ProgressMapStepComponent = ProgressMapStep;
export default withStyle(styles)(ProgressMapStep);