import React from 'react';
import PropTypes from 'prop-types';
import cn from 'classnames';

const Note = ({
  message,
  className,
  px,
  children
}) => {
  const content = children || message;
  return /*#__PURE__*/React.createElement(React.Fragment, null, content ? /*#__PURE__*/React.createElement("div", {
    className: cn('note overflow-y-auto b-1 w-fill border-gray-light font-size-md py-3 bg-yellow-light min-height-50 max-height-200', `px-${px}`, className)
  }, content) : null);
};

Note.defaultProps = {
  children: null,
  message: '',
  className: '',
  px: 6
};
Note.propTypes = {
  /** Note content children */
  children: PropTypes.node,

  /** Additional class name for Note */
  className: PropTypes.string,

  /** Note message, if none provided will not render anything - DEPRECATED - use children */
  message: PropTypes.string,

  /** padding x for note - ( 6===30 9===45) */
  px: PropTypes.oneOf([6, 9])
};
export default Note;