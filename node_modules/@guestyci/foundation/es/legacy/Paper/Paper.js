import _extends from "@babel/runtime/helpers/esm/extends";
import _objectWithoutPropertiesLoose from "@babel/runtime/helpers/esm/objectWithoutPropertiesLoose";
import React from 'react';
import PropTypes from 'prop-types';
import cn from 'classnames';
const Paper = React.forwardRef((_ref, ref) => {
  let {
    className,
    bgColor,
    style,
    elevation,
    borderRadius,
    children
  } = _ref,
      props = _objectWithoutPropertiesLoose(_ref, ["className", "bgColor", "style", "elevation", "borderRadius", "children"]);

  return /*#__PURE__*/React.createElement("div", _extends({
    ref: ref,
    className: cn(className, `box-shadow-${elevation}`, `bg-${bgColor}`, `border-radius-${borderRadius}`),
    style: style
  }, props), children);
});
Paper.defaultProps = {
  bgColor: 'white',
  elevation: 1,
  borderRadius: 0,
  className: '',
  style: {}
};
Paper.propTypes = {
  /** Paper inner content */
  children: PropTypes.node.isRequired,

  /** Paper background color */
  bgColor: PropTypes.string,

  /** Paper box shadow elevation 1-10 */
  elevation: PropTypes.number,

  /** Paper roundness  0 - 25 leaps of 5  */
  borderRadius: PropTypes.number,

  /** Paper additional classes */
  className: PropTypes.string,

  /** Paper additional styles */
  style: PropTypes.shape()
};
export default Paper;