import React from 'react';
import cn from 'classnames';
import Input from "../Input";
import DropdownClearButton from "./DropdownClearButton";
import createStyles from "../createStyles";
const useStyles = createStyles(theme => ({
  root: {
    margin: `${theme.spacer(2)}px 0`
  }
}), {
  name: 'DropdownSearch'
});
const DropdownSearch = React.forwardRef(function DropdownSearchInput({
  value,
  onChange,
  placeholder = 'Search...',
  className,
  onClear
}, ref) {
  const classes = useStyles();
  return /*#__PURE__*/React.createElement(Input, {
    ref: ref,
    value: value,
    onChange: onChange,
    placeholder: placeholder,
    className: cn(classes.root, className),
    suffix: /*#__PURE__*/React.createElement(DropdownClearButton, {
      show: !!value,
      asSuffix: true,
      onClick: onClear
    })
  });
});
DropdownSearch.displayName = 'DropdownSearchInput';
export default DropdownSearch;