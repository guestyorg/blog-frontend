import React from 'react';
import cn from 'classnames';
import PropTypes from 'prop-types';
import withStyle from 'react-jss';
const styles = {
  stepDivider: {
    position: 'relative'
  },
  stepDividerLine: props => ({
    height: '1px',
    width: `${props.width}px`
  }),
  stepDividerText: {
    position: 'absolute',
    bottom: '0',
    left: '50%',
    transform: 'translateX(-50%)',
    'text-align': 'center'
  }
};

const StepDivider = ({
  text,
  color,
  classes
}) => /*#__PURE__*/React.createElement("div", {
  className: ('flex-1', classes.stepDivider),
  "data-qa": "step-divider"
}, /*#__PURE__*/React.createElement("span", {
  className: cn(classes.stepDividerText, `text-${color}`)
}, text), /*#__PURE__*/React.createElement("div", {
  className: cn(classes.stepDividerLine, `bg-${color}`)
}));

StepDivider.defaultProps = {
  text: '',
  color: 'gray',
  width: 19
};
StepDivider.propTypes = {
  text: PropTypes.string,
  color: PropTypes.string,
  width: PropTypes.number
};
/** Work around to fix storybook HOC propTypes bug  - DO NOT IMPORT THIS */

export const StepDividerComponent = StepDivider;
export default withStyle(styles)(StepDivider);