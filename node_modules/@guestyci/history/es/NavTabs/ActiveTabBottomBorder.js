import _extends from "@babel/runtime/helpers/esm/extends";
import React from 'react';
import cn from 'classnames';
import withStyles from 'react-jss';
const styles = {
  border: {
    transition: 'left 225ms cubic-bezier(0, 0, 0.2, 1) 0ms, width 225ms cubic-bezier(0, 0, 0.2, 1)'
  }
};

const ActiveTabBottomBorder = ({
  classes,
  width,
  position,
  style,
  noAnimate
}) => /*#__PURE__*/React.createElement("div", {
  className: cn('absolute bottom-0 bb-2 border-blue text-blue', !noAnimate && ['transition-left', classes.border]),
  style: _extends({
    width,
    left: position
  }, style)
});

export default withStyles(styles)(ActiveTabBottomBorder);