import React from 'react';
import { JssProvider } from 'react-jss';

const createGenerateId = rule => `${rule.key}__test__selector`;

const MockJssProvider = ({
  children
}) => React.createElement(JssProvider, {
  generateId: createGenerateId
}, children);

export default MockJssProvider;