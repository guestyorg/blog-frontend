import React from 'react';
import { mount } from "../utils";
import MockJssProvider from "./MockJssProvider";
export default (function (component, useStyles) {
  var classes = {};
  var Comp = React.cloneElement(component);
  var props = Comp.props;
  /**
   * @return {null}
   */

  function Shell() {
    classes = useStyles(props);
    return null;
  }

  mount(React.createElement(MockJssProvider, null, React.createElement(Shell, null)));
  return classes;
});