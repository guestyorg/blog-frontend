import React from 'react';
import { mount } from "../utils";
import MockJssProvider from "./MockJssProvider";
export default ((component, useStyles) => {
  let classes = {};
  const Comp = React.cloneElement(component);
  const {
    props
  } = Comp;
  /**
   * @return {null}
   */

  function Shell() {
    classes = useStyles(props);
    return null;
  }

  mount(React.createElement(MockJssProvider, null, React.createElement(Shell, null)));
  return classes;
});