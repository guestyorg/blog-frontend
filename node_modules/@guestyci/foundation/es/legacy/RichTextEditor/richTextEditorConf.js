import _extends from "@babel/runtime/helpers/esm/extends";
export const colors = ['black', '#444', '#666', '#999', '#ccc', '#eee', '#f3f3f3', 'white', 'red', 'orange', 'yellow', 'lime', 'cyan', 'blue', 'purple', 'magenta'];
const fonts = [{
  label: 'Small',
  value: '9px',
  key: 'small-text'
}, {
  label: 'Normal',
  value: '12px',
  key: 'normal-text'
}, {
  label: 'Large',
  value: '24px',
  key: 'large-text'
}, {
  label: 'Huge',
  value: '36px',
  key: 'huge-text'
}];
export const customStyleMap = _extends({}, fonts.reduce((acc, {
  key,
  value
}) => {
  acc[key] = {
    fontSize: value
  };
  return acc;
}, {}), {}, colors.reduce((acc, color) => {
  acc[color] = {
    color
  };
  return acc;
}, {}));
export const customStyleConvertMap = _extends({}, colors.reduce((acc, color) => {
  acc[color] = color;
  return acc;
}, {}), {}, fonts.reduce((acc, {
  value,
  key
}) => {
  acc[value] = key;
  return acc;
}, {}));
export const fontSize = fonts.map(({
  label,
  key
}) => ({
  label,
  value: key
}));