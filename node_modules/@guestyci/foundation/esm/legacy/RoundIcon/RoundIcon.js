import _extends from "@babel/runtime/helpers/esm/extends";
import _defineProperty from "@babel/runtime/helpers/esm/defineProperty";
import React from 'react';
import PropTypes from 'prop-types';
import cn from 'classnames';
import Icon from "../Icon";
var roundIconStyle = {
  borderRadius: '100%'
};
var iconSizes = {
  sm: 25,
  md: 40,
  lg: 55
};

var RoundIcon = function RoundIcon(_ref) {
  var iconWidth = _ref.iconWidth,
      iconHeight = _ref.iconHeight,
      color = _ref.color,
      bgColor = _ref.bgColor,
      size = _ref.size,
      className = _ref.className,
      icon = _ref.icon,
      svg = _ref.svg,
      containerClassName = _ref.containerClassName,
      style = _ref.style,
      containerStyle = _ref.containerStyle;
  return /*#__PURE__*/React.createElement("div", {
    className: cn('flex-center', "icon-wrapper-".concat(size), _defineProperty({}, "bg-".concat(bgColor), bgColor), containerClassName),
    style: _extends({}, roundIconStyle, {}, containerStyle),
    "data-qa": "round-icon"
  }, /*#__PURE__*/React.createElement(Icon, {
    width: iconWidth || iconSizes[size],
    height: iconHeight || iconSizes[size],
    icon: icon,
    svg: svg,
    color: color,
    style: style,
    className: cn(className, 'icon-round')
  }));
};

RoundIcon.defaultProps = {
  size: 'md',
  iconWidth: null,
  iconHeight: null,
  className: '',
  containerClassName: '',
  bgColor: '',
  color: '',
  containerStyle: {},
  style: {}
};
RoundIcon.propTypes = {
  /** Round Icon component size - allowing (xs, md, lg) */
  size: PropTypes.oneOf(['xs', 'md', 'lg']),

  /** Icon width  - defaults to Icon width default */
  iconWidth: PropTypes.number,

  /** Icon height  - defaults to Icon height default */
  iconHeight: PropTypes.number,

  /** Icon className  */
  className: PropTypes.string,

  /** Icon style */
  style: PropTypes.shape(),

  /** Icon container className  */
  containerClassName: PropTypes.string,

  /** Icon container style  */
  containerStyle: PropTypes.shape(),

  /** Round Icon background color */
  bgColor: PropTypes.string,

  /** inner Icon fill color - defaults to Icon color default  */
  color: PropTypes.string,

  /** Round Icon Icon name - available from the icon library */
  icon: PropTypes.string,

  /** The svg icon to render */
  svg: PropTypes.oneOfType([PropTypes.shape(), PropTypes.func])
};
export default RoundIcon;