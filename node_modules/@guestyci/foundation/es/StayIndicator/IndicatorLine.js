import React from 'react';
import { componentThickness, lineWidth } from "./helpers";

const IndicatorLine = ({
  className,
  vertical
}) => {
  const viewBox = `0 0 ${componentThickness} ${componentThickness}`;
  const componentMiddleHeight = componentThickness / 2;
  const d = !vertical ? `M 0 ${componentMiddleHeight} L ${componentThickness} ${componentMiddleHeight}` : `M ${componentMiddleHeight} 0 L ${componentMiddleHeight} ${componentThickness}`;
  return /*#__PURE__*/React.createElement("svg", {
    className: className,
    viewBox: viewBox,
    preserveAspectRatio: "none",
    "data-qa": "indicator-line"
  }, /*#__PURE__*/React.createElement("path", {
    fill: "none",
    vectorEffect: "non-scaling-stroke",
    strokeWidth: lineWidth,
    d: d
  }));
};

export default IndicatorLine;